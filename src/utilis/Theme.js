import { createTheme } from "@material-ui/core";

const Theme = createTheme({
    palette: {
      primary: {
        main: "#030303",
      },
      secondary: {
        main: "#273C2C",
      },
      common: {
        drawer: "#030303",
      },
    },
  });
  
  export default Theme;